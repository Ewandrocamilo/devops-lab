apiVersion: v1
kind: Pod

metadata:
  name: config-test
  namespace: devops-lab

spec:
  containers:
  - name: teste
    image: busybox

    command:
    - sleep
    - "3600"

    envFrom:
    - configMapRef:
        name: app-config
